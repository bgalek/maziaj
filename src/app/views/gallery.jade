#showcase
    .container
        .row
            .col-lg-12.help
                p.text-center In Doodler your task is to describe someone's drawing or draw something that has been already described. The drawer sees only the latest description and the describer - the last drawing. It's kind of charades but everybody give wrong answers so as a result we get a chain of funny and absurd associations. Oh boy, that is fun as hell!
                cite Still don't get the point? Just give it a try.
                    br
                    strong Seriously. That's simple.
                a.btn.btn-danger.btn-lg.play(href="#/play") Play now!
            //.col-lg-3.login-form
                form(action="/login", method="POST")
                    fieldset
                        a.play(href="#/play") Graj!
                        span.divider
                            small lub
                        input(type="text", name="username", placeholder="email...", required="required", autofocus="autofocus")
                        input(type="password", name="password",  placeholder="hasło...", required="required")
                        input(type="submit", value="zaloguj się")
                a.btn.btn-facebook(href="login/auth/facebook") zaloguj się przez
                    br
                    strong facebook
                a.btn.btn-google(href="login/auth/google") zaloguj się przez
                    br
                    strong google
#gallery.container
    .row
        .col-lg-12
            h2.text-center Recent chains
                small fresh take of absurdity
            
        .col-md-3.portfolio-item(data-ng-repeat="chain in chains.list")
            article.doodle
                .doodle-details
                    .counter-doodlers
                        i.fa.fa-users
                        strong 23
                        |  doodlers
                    .latest-doodle-date
                        i.fa.fa-clock-o
                        | {{ chain.creation_date | todayDeltaFilter }}
                .clearfix
                .doodle-contents
                    a(href="#/chain/{{chain._id}}")
                        img.doodle-image.img-responsive(data-ng-show="{{ chain.image.length > 0 }}", data-ng-src="{{ chain.image }}", alt="Drawing author: {{ doodle.author }}")
                        div.doodle-text(data-ng-show="{{ chain.text.length > 0 }}") {{ chain.text }}
        .col-lg-12.text-center(style="padding: 20px 0;")
            pagination(style="margin: 0px;",data-ng-if="chains.list.length > 0", total-items="chains.totalItems", ng-model="chains.currentPage", ng-change="chains.pageChanged()")
